import React from 'react';
import Button from './button';

class HomePage extends React.Component {
    render() {
        return (
            <div>
                <h1>Empty Landing Page</h1>
                <Button buttonTitle='Go to Sign Up page'/>
            </div>
        );
    }
}

export default HomePage;
